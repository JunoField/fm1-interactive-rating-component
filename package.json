{
  "devDependencies": {
    "@parcel/transformer-sass": "^2.5.0",
    "parcel": "^2.5.0"
  },
  
  "scripts": {
    "start": "parcel ./index.html",
    "prebuild-dev": "shx rm -rf dist/*",
    "build-dev": "parcel build /index.html --no-minify",
    "prebuild": "shx rm -rf dist/*",
    "build": "parcel build ./index.html",
    "push-deploy": "push-dir --dir=dist --branch=deploy --cleanup --verbose"
  }
}
